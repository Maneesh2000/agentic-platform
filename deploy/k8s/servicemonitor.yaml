# Prometheus Operator scrape config for worker metrics.
apiVersion: monitoring.coreos.com/v1
kind: ServiceMonitor
metadata:
  name: chat-assistant
  labels:
    app: chat-assistant
spec:
  selector:
    matchLabels:
      app: chat-assistant
  endpoints:
    - port: metrics
      path: /metrics
      interval: 15s
